# Cell 4: Array Initialization Techniques
"""
## Array Initialization Techniques

Various ways to create arrays:
"""
# From existing data
a = np.array([1, 2, 3])

# Special arrays
zeros = np.zeros(5)          # [0., 0., 0., 0., 0.]
ones = np.ones((2, 3))       # 2x3 array of 1s
identity = np.eye(4)         # 4x4 identity matrix
full = np.full((3, 3), 7)    # 3x3 array filled with 7
random = np.random.rand(2, 2) # Uniform random [0,1)
randint = np.random.randint(0, 10, (3, 3)) # Random integers

print("Zeros:\n", zeros)
print("\nOnes:\n", ones)
print("\nIdentity:\n", identity)
print("\nFull:\n", full)
print("\nRandom:\n", random)
print("\nRandom Integers:\n", randint)
